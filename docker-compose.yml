services:
  gmarket:
    build: .
    ports:
      - "3000:3000"
    environment:
      - PROJECT_ROOT=/app
    volumes:
      - results:/app/results
      - tokens:/app/.tokens
    restart: unless-stopped

volumes:
  results:
  tokens:
